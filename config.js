const config = {
    uname: 'root',
    upass: 'root', //windows leave blank
    port: 8889, //windows 3306
    host: 'localhost',
    db: 'bizcard'

}

// make this a public file our app can read
module.exports = config;